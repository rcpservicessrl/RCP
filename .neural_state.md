# .neural_state.md — Estado de sesión RCP Services

**Última actualización:** 2026-07-08
**Sesión actual:** Carga optimizada + Copy simplificado + Corrección de Rutas de Imágenes + Directivas de Aprendizaje.

## 🚨 DIRECTIVA DE APRENDIZAJE CONTINUO (Obligatoria para toda IA)
- **Regla de oro:** Cada vez que finalice cualquier operación, cambio, refactorización o corrección, la IA debe documentar de forma inmediata en este archivo los cambios realizados, las razones técnicas y los aprendizajes.
- **Propósito:** Evitar la regresión de cambios, impedir que se "corrijan" partes funcionales por error y garantizar que la capa neuronal mantenga memoria e históricos consistentes entre sesiones.

---

## 🚨 UBICACIÓN DEL PROYECTO
- **Ruta física del repositorio:** `C:\RCP\RCP Services\Sitio-Web\`
- **Ruta física de documentación / Obsidian vault:** `C:\RCP\RCP Services\` (fuera de OneDrive para prevenir race conditions y reversiones involuntarias).
- **Control de Versiones:** Conexión activa con `origin → github.com/rcpservicessrl/RCP.git` en rama `master`.

---

## Completado esta sesión (2026-07-08)

- [x] **Optimización de carga (FOUC Eliminado):** 
  - Se movió el stylesheet de `/styles.css` del final del body al `<head>` en [BaseLayout.astro](file:///C:/RCP/RCP%20Services/Sitio-Web/src/layouts/BaseLayout.astro#L135-L137). El HTML renderiza estilizado de forma inmediata.
- [x] **Simplificación de Copy en landing:**
  - Reducción del subtítulo del Hero a 20 palabras.
  - Reducción de descripciones del Ecosistema Soberano a máximo 2 líneas.
  - Tabla comparativa reformateada con claims cortos (3-5 palabras).
  - Resumen del párrafo de "Junta Directiva Externa" a 39 palabras en total.
- [x] **Agrupación estructural de las 11 Capas:**
  - En lugar de lista plana, se estructuró en 3 categorías visuales (*Infraestructura Core*, *Capa de Inteligencia* y *Gestión de Negocio*) en [index.astro](file:///C:/RCP/RCP%20Services/Sitio-Web/src/pages/index.astro).
- [x] **Solución de Imágenes Rotas en Rutas Anidadas:**
  - Se modificaron [script.js](file:///C:/RCP/RCP%20Services/Sitio-Web/public/script.js#L166) y [tienda.js](file:///C:/RCP/RCP%20Services/Sitio-Web/public/tienda.js#L169). Todas las rutas de imágenes relativas (como `assets/products/` y `Logo RCP Services.png`) se convirtieron a rutas raíz-relativas con barra diagonal inicial (ej: `/assets/products/` y `/Logo RCP Services.png`). Esto evita que se rompan las imágenes en rutas anidadas como `/tienda/` o `/portal/`.
- [x] **Git Push Completado:**
  - Cambios compilados con `npm run build` y subidos a producción usando el token configurado en el `.env`.

---

## Cambios de branding históricos (Preservados en master)
- Placeholders de teléfonos corregidos a `829-806-8092` en `carreras.astro`, `dashboard.astro` y `onboarding.astro`.
- Correos corregidos a `info@rcp.services` en `dashboard.astro`.
- Comentario de webhook de n8n neutralizado a "Disparador del pipeline propietario".

---

## Lo que NO se debe modificar (Reglas de persistencia)
- **`CONFIDENTIAL_KEYWORDS`** en `script.js` y `cloud_function/main.py` (filtro de seguridad del chatbot).
- **Nombres de campos del esquema en Supabase** (`sincronizado_odoo`, `odoo_sale_order_id` en `dashboard.astro`) porque romperían la comunicación con la base de datos real.
- **System prompt** del chatbot en `cloud_function/main.py:97` que restringe revelar tecnologías internas.

---

## Próximos pasos recomendados
1. **RBAC y Esquemas de Supabase:** Aplicar los scripts `.sql` pendientes del directorio de Supabase.
2. **Igualación e integración de i18n:** Traducir los keys ES/EN faltantes sugeridos por `validate.js`.

# .neural_state.md — Estado de sesión RCP Services

**Última actualización:** 2026-06-25
**Sesión actual:** Endurecimiento de seguridad + RBAC + eliminación de simulaciones.

## Completado esta sesión
- [x] Migración repo a `C:\Users\balmi\Projects\rcp-services-web`, token GitHub limpiado, original eliminado.
- [x] Fase 1A–1C: seguridad portal.html, RLS clientes, Cloud Functions (rate limit real + timeout Odoo).
- [x] Fase 2: RBAC completo (5 tablas + funciones) + migración dashboard.html a `get_user_role` RPC.
- [x] Fase 3: tablas `marketing_metrics` + `client_tramites`, RPC `dashboard_overview`, dashboard migra a datos reales, eliminadas todas las fabricaciones.

## Próximos pasos sugeridos (otra sesión)
1. **Probar en Supabase real:** aplicar `02_rbac_schema.sql` y `03_real_metrics_schema.sql` en orden en el SQL Editor y correr los smoke tests del bloque de verificación.
2. **n8n:** crear flujo de importación Meta/Google Ads → `marketing_metrics` (tabla lista, columna `source`).
3. **Granularidad semanal marketing:** si se quiere el SVG de tendencia CPL, añadir columna `period_week` o tabla separada.
4. **UI gestión RBAC:** no existe pantalla admin para asignar roles (hoy se hace vía SQL/Supabase Studio). Considerar panel en dashboard.html con permiso `rbac.manage`.
5. **Seed datos reales:** el dashboard mostrará "—" en marketing hasta que haya filas en `marketing_metrics`; crear inserts de prueba.

## Notas técnicas
- `dashboard_overview` RPC: `p_global=true` agrega SUM/AVG sobre todos los clientes (vista admin); `p_global=false` datos de un cliente por email.
- `chart_data` ahora se deriva de `ordenes` WHERE status='paid' agrupado por mes (últimos 7 meses). Si no hay ordenes pagadas → array de ceros.
- `pagos` viene del mismo mapeo de ordenes paid (order_number, fecha, items concatenados, total, payment_method, flag sync odoo).
- RLS de tablas nuevas usa `has_permission()` de Fase 2 (respeta facturacion.read/write, dashboard.read/admin_view, rbac.manage).

## Estado de verificación
- JS de dashboard.html: parsea sin errores (1 bloque script validado).
- Sin referencias residuales a `simClient`, `Math.random`, `payload.chart_data`, `payload.pagos`, ni "(Simulación)".
- Claves RPC ↔ render consistentes (tramites con fallback a legacy).

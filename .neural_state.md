# .neural_state.md — Estado de sesión RCP Services

**Última actualización:** 2026-08-12
**Sesión actual:** RCP Services 6.0-RC2 reconciliada con el saneamiento productivo previo; candidato Next/Vercel validado localmente, con staging, integraciones y corte todavía sujetos a gates.

## 🚨 DIRECTIVA DE APRENDIZAJE CONTINUO (Obligatoria para toda IA)
- **Regla de oro:** Cada vez que finalice cualquier operación, cambio, refactorización o corrección, la IA debe documentar de forma inmediata en este archivo los cambios realizados, las razones técnicas y los aprendizajes.
- **Propósito:** Evitar regresiones y mantener memoria consistente entre sesiones.

## 🚨 UBICACIÓN DEL PROYECTO
- **Repositorio:** `C:\RCP\RCP Services\Sitio-Web\`
- **Vault Obsidian:** `C:\RCP\RCP Services\`
- **Control de versiones:** `origin → github.com/rcpservicessrl/RCP.git`, rama `master`.

## 🌉 CONEXIÓN DE GRAFO NEURONAL
- **Conversación enlazada:** `Guía Profesional Contabilidad RD` (ID `7b1b6664-d929-49c5-9fcc-6f6d2c267919`).
- **Workspace:** `g:/Mi unidad/RCP Documentos`.
- **Nodo:** `knowledge_graph/NODO_GUIA_CONTABILIDAD_RD.md`.
- **Índice:** `knowledge_graph/graphify_index.json`.
- **Matriz central:** `g:/Mi unidad/RCP Documentos/knowledge_graph/000_MATRIZ_NEURONAL_CENTRAL.md`.
- **Motor enlazado:** paquete `rcp_audit` en `g:/Mi unidad/RCP Documentos/src/rcp_audit`.
- **Advertencia de vigencia:** `graphify-out/graph.json` todavía mezcla nodos Astro históricos con documentación Blueprint 5. Para implementación web vigente mandan `app/`, `components/`, `lib/`, `docs/README.md` y este estado hasta completar una reconstrucción semántica incremental del grafo.

## Arquitectura compartida RCP Services
- **Producto canónico:** `C:\RCP\RCP Services\Sitio-Web`.
- **Producción vigente antes del corte:** `https://rcp.services`, publicada por GitHub Pages/Astro.
- **Destino aprobado RC2:** Next.js sobre Vercel Pro con Node 24; Route 53 continúa autoritativo. Cloudflare Workers/OpenNext queda como alternativa portable fuera del corte.
- **Pool corporativo:** Supabase `wpfovxgbennpgydbellw`, organización `RCP Services`.
- **Esquema actual:** `rcp_services`, activo y verificado; los 13 objetos legados del sitio ya no permanecen en `public`.
- **Pool de clientes:** Supabase `ietytetmsksqpvbotjes`, organización `RCP Services clientes`.
- No crear tablas de clientes en el esquema corporativo `public`.
- Consultar `RCP_SERVICES_SHARED_ARCHITECTURE.md` antes de modificar Supabase.

## Completado en sesiones previas

### Fuente de Verdad 6.0-RC2 y web Vercel — 2026-08-12
- Dirección General aprobó D-01 a D-11. RC2 separa Evaluación Inicial RCP 360° sin costo del Diagnóstico RCP 360 pagado; mantiene Publicidad como pilar y “Publicidad 360” como descriptor; oculta legal; convierte e-CF en guía educativa; y conserva CRM como herramienta interna.
- El dominio comercial usa `CommercialState`, `TechnicalMaturity`, `regulated`, `requiresProfessionalReview` y `selectable`. Catálogo, buscador, JSON-LD, índice y formularios consumen proyecciones filtradas y el servidor vuelve a validar selecciones.
- Se publican seis soluciones tecnológicas en lenguaje humano y las 16 siglas quedan como glosario SSR. Portal salió de navegación, buscador, Pulso y footer; sus rutas conservan `noindex` y un aviso sin acceso.
- La captación del piloto usa `RCP_INTAKE_DELIVERY_MODE=email`: Resend debe confirmar proveedor antes de `recorded: true`. El modo CRM queda preparado con HMAC, timestamp e idempotencia, sin doble escritura.
- Vercel con Node 24 es el destino aprobado; Route 53 sigue autoritativo y OpenNext/Cloudflare permanece como salida portable. TypeScript, 42 pruebas y build Next de 59 rutas pasan localmente.
- Los lockups secundarios RC1 se extrajeron sin modificar y sus hashes canónicos quedaron probados. Las tres posturas aprobadas de Pulso permanecen inmutables.

### Blueprint 5 — 2026-08-11
- Worktree aislado `C:\RCP\.worktrees\rcp-next-blueprint5`, rama `codex/rcp-next-blueprint-5`; checkout productivo preservado.
- Next.js App Router, tres pilares, catálogo completo, tecnología transversal, método RCP, ES/EN, claro/oscuro, búsqueda, música y Pulso oficial.
- La Red de Especialistas usa las rutas canónicas `/especialistas`, `/en/specialists`, `/especialistas/postular` y `/en/specialists/apply`. La postulación inicial no acepta archivos ni documentos sensibles; `/api/specialist-applications` solo confirma éxito después de una confirmación explícita del CRM configurado mediante variables server-side y conserva el formulario con salida a `talento@rcp.services` ante fallos.
- Flujo búsqueda → catálogo → diagnóstico → API → referencia/WhatsApp verificado con datos sintéticos; CRM y Resend permanecen opcionales de servidor.
- Metadata Astro conflictiva archivada; sitemap vigente contiene 24 rutas públicas con respuesta 200 local.
- Menú móvil corregido mediante portal al `body`; foco, Escape, 390 × 844 y 1440 × 900 aprobados.
- TypeScript, pruebas, Next build, bundle OpenNext, dry-run de Wrangler y preview local Workerd aprobados. La fecha de compatibilidad ejecutable quedó fijada en `2026-08-08`. Ver `docs/VERIFICATION_REPORT_2026-08-11.md`.
- CI migrado a pnpm; despliegue Cloudflare manual y protegido. Sin DNS, Portal, Supabase ni producción modificados.

### 0. Identidad visual y experiencia responsive — 2026-07-31
- El catálogo oficial usa `logo_rcp_fondo_oscuro.svg`,
  `logo_rcp_fondo_claro.svg`, `logo_rcp_simbolo.svg` y sus derivados.
- El header, footer, portal y dashboard tienen escalas mínimas documentadas en
  `STYLE_GUIDE.md`; no reducir el logo para resolver saturación del menú.
- La portada utiliza navegación de exploración, tarjetas visuales con
  desplazamiento horizontal en móvil, FAQ progresiva y un hero con el símbolo
  oficial.
- El contenido SEO permanece en HTML: canonical, metadatos, Open Graph,
  JSON-LD Organization/WebSite/FAQPage y jerarquía semántica.
- Breakpoints de referencia: escritorio `1440×900`, tablet `768×1024` y móvil
  `390×844`, todos sin desbordamiento horizontal.
- Decisión detallada: `knowledge_graph/NODO_EXPERIENCIA_VISUAL_RESPONSIVE_2026-07-31.md`.

### 1. Migración y conectividad a Supabase
- Conexión al Pooler IPv4 de Supabase con SSL.
- Migración `07_shop_expansion_migration.sql` aplicada en producción.

### 2. Panel administrativo CRUD expandido
- Gestión de cupones, imágenes, SKU y especificaciones técnicas.

### 3. Tienda pública y checkout
- El catálogo remoto es la única autoridad; se eliminó el catálogo estático y el caché de precios.
- El checkout simulado fue sustituido por cotización basada solo en SKU, validada contra Supabase, con salida por WhatsApp o `info@rcp.services` (Zoho Mail).
- No se procesan pagos, no se crean órdenes y las pasarelas están fuera de alcance actual.

### 4. Seguridad, privacidad y evidencia — 2026-08-05
- Migración remota `20260805023000` separa las políticas de lectura de productos para `anon` y `authenticated`; prueba pública devuelve activos y oculta inactivos.
- Respaldo previo de esquema y datos guardado fuera del repositorio de entrega.
- Astro actualizado a 7.1.6, CI a Node 22 y auditoría npm en cero vulnerabilidades.
- Diagnóstico 360 ya no solicita PII, transmite respuestas ni inventa pérdidas financieras.
- Claims no respaldados, testimonios ficticios y afirmaciones de pagos fueron retirados.
- Políticas de privacidad, términos, cookies y reembolsos incorporan RCP Services SRL, RNC 132-147103, Av. Rómulo Betancourt 1302, Bella Vista, Santo Domingo, y Zoho Mail como canal de correo.
- GA4 usa consentimiento por defecto denegado; solo la analítica puede habilitarse. Publicidad, personalización y datos publicitarios permanecen denegados.
- Documentación de auditoría, riesgos, datos, marca, SEO, seguridad, lanzamiento, monitoreo y rollback vive en `docs/`.

### 5. Validación pendiente
- Staging independiente creado y verificado en Vercel (`https://rcp-services-staging.vercel.app`), sin DNS y con `PUBLIC_DEPLOYMENT_ENV=staging`; responde 200, muestra aviso de staging y `noindex`; falta UAT funcional.
- Falta acceso DNS/Search Console, prueba de recepción de Zoho Mail y revisión de eventos GA4 sin PII.
- Producción web solo puede cambiar mediante PR revisado y merge aprobado a `master`.
- La propuesta de inversión está `noindex`, pero requiere autenticación si debe ser confidencial.

### 6. Hardening y corte productivo del sitio corporativo — 2026-08-10
- Todos los clientes Supabase del navegador y las consultas REST del sitio apuntan explícitamente al esquema corporativo `rcp_services`; la migración Matrix `20260810160000` está aplicada y registrada en el ledger remoto.
- El portal deja de crear perfiles, códigos de acceso o contraseñas reutilizables desde el navegador. El alta y la sesión quedan bajo Supabase Auth y el onboarding autenticado.
- El perfil corporativo se aprovisiona mediante `ensure_client_profile()` después de autenticar; no se instala un trigger global sobre `auth.users`, porque Auth es compartido por varios productos corporativos.
- El onboarding pasa de `onboarding` a `review`; `pending_activation` queda fuera del flujo nuevo para evitar el bucle legado basado en códigos.
- El flujo público de reservas ya no lee ni modifica `clientes`; únicamente entrega el lead por el webhook existente y conserva su cola local sin sincronización privilegiada.
- Carreras escribe en `postulaciones`, contacto escribe en `contactos` y ambos guardan rutas privadas con UUID para evitar nombres predecibles y URLs públicas de documentos.
- El dashboard elimina la edición y exposición de contraseñas/códigos de acceso, escapa contenido dinámico y fija Realtime al esquema corporativo.
- `src/scripts/dashboard.js` se eliminó porque era una copia sin consumidor; la autoridad cargada por `DashboardLayout.astro` es `public/scripts/dashboard.js`.
- Contratos automatizados bloquean regresiones a `public`, flujos de identidad anónimos, códigos reutilizables, tablas de admisión incorrectas y rutas públicas de CV.
- Verificación local: 10 pruebas de política, 3 pruebas sobre `dist`, build Astro completo y `npm audit` con 0 vulnerabilidades usando Node 26.2.0.
- Respaldo DPAPI externo validado antes del DDL: 13 tablas, 72 productos y seis clientes, con SHA-256 documentado en RCP Matrix.
- Verificación remota posterior: 15 tablas bajo RLS forzado, cero tablas legadas del sitio en `public`, seis clientes, 18 trámites, 72 productos, siete asignaciones de rol, un superadministrador activo y dos tablas en Realtime.
- Producción desplegada por GitHub Pages desde el merge `d76398e` (PR #5, ejecución `31398092559`); `https://rcp.services/tienda/` renderiza los 72 productos y no genera errores de consola.
- La evidencia operativa y el rollback pertenecen a `RCP-Matrix/supabase/corporate/`; no se debe recrear DDL desde este repositorio.
- Los nueve SQL de `supabase/snippets/` son instantáneas históricas y ahora fallan de forma cerrada antes de cualquier DDL; nunca deben ejecutarse contra producción ni tratarse como migraciones vigentes.
- Siguientes compuertas del sitio: UAT autenticado de portal/dashboard, prueba de recepción Zoho, verificación de eventos GA4 sin PII y decisión sobre autenticación de la propuesta de inversión.

## Lo que NO se debe modificar
- `CONFIDENTIAL_KEYWORDS` en `script.js` y `cloud_function/main.py`.
- Campos Supabase `sincronizado_odoo` y `odoo_sale_order_id`.
- System prompt del chatbot en `cloud_function/main.py`.
- Archivos curados en `public/assets/products/`.

# Archived Astro public metadata

These files document the pre-Blueprint 5 public metadata. They are intentionally outside `public/` because Next.js now generates `/robots.txt`, `/sitemap.xml`, and `/llms.txt` from the current source of truth.
